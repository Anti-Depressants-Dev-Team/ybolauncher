<UserControl
    x:Class="Launcher.App.Controls.SearchResultsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="using:Launcher.App.Controls"
    xmlns:vm="using:Launcher.App.ViewModels"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid Padding="12,8,12,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="4,0,4,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Library.SearchSummary, Mode=OneWay}" />

            <ToggleButton
                Grid.Column="1"
                AutomationProperties.Name="Limit search to the current tab"
                Content="{x:Bind Library.SearchScopeLabel, Mode=OneWay}"
                IsChecked="{x:Bind Library.SearchCurrentTabOnly, Mode=TwoWay}"
                ToolTipService.ToolTip="Search all tabs, or only the selected one" />
        </Grid>

        <ListView
            x:Name="Results"
            Grid.Row="1"
            AutomationProperties.Name="Search results"
            IsItemClickEnabled="True"
            ItemClick="OnResultClick"
            ItemsSource="{x:Bind Library.SearchResults}"
            SelectionChanged="OnSelectionChanged"
            SelectionMode="Single">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:SearchResultViewModel">
                    <Grid Height="48" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Image
                            Grid.Column="0"
                            Width="32"
                            Height="32"
                            AutomationProperties.AccessibilityView="Raw"
                            Source="{x:Bind Icon}" />

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <!-- Inlines are rebuilt from the segments so the matched
                                 characters are emphasised. -->
                            <TextBlock
                                ctrl:HighlightText.Segments="{x:Bind NameSegments}"
                                Style="{StaticResource BodyTextBlockStyle}"
                                TextTrimming="CharacterEllipsis" />

                            <TextBlock
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind Detail}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel
            Grid.Row="1"
            MaxWidth="420"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            x:Name="EmptyState"
            Visibility="Collapsed">

            <FontIcon
                AutomationProperties.AccessibilityView="Raw"
                FontSize="32"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                Glyph="&#xE721;" />

            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource PageSubtitleTextStyle}"
                Text="Nothing matched. Try fewer letters, or switch the scope to all tabs."
                TextAlignment="Center" />
        </StackPanel>
    </Grid>

</UserControl>
